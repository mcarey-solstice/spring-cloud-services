---
spring:
  profile: dev

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF

---
spring:
  profile: local

server:
  port: 8761

---
spring:
  profile: docker
